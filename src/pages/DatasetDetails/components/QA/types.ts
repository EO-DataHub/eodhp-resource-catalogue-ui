export interface PerformanceSpecRow {
  metric: string;
  value: string;
  verified: string;
  result: string;
  lastChecked: string;
}

export interface QuadAttribute {
  predicate: string;
  object: string;
}
